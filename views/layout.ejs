<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title><%= appTitle %></title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 480px)" href="/stylesheets/smartphone.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (min-width: 530px) and (min-device-width: 481px)" href="/stylesheets/wide.css" />
	</head>
	<body>
		<div id="page-wrap">

			<header class="main" id="h1">
				<% if(!session.user) { %>
					<span class="right"><a href="/login">Login</a></span>
				<% } else { %>
					<span class="right"><%= session.user.username %> <a href="/logout">Logout</a></span>
				<% } %>

				<h2><%= intro %></h2>
			</header>

			<nav class="main" id="n1" role="navigation">
				<table>
					<tr><td><a href="/">Inicio</a></td></tr>
					<tr><td><a href="/quizes">Jugar!</a></td></tr>
					<% if(session.user) { %>
						<tr><td><a href="/quizes/new">Crear pregunta</a></td></tr>
					<% } %>
					<tr><td>-</td></tr>
					<tr><td><a href="/author">Créditos</a></td></tr>
				</table>
			</nav>

			<section class="main" id="s1">
				<% if(errors.length) { %>
					<span id="errorMsgs">
						Corrija lo siguiente:<br />
						<% for(var i = 0; i < errors.length; i++) { %>
							<spam><%= errors[i].message %></spam><br />
						<% } %>
					</span>
				<% } %>
				<br />
				<%- body %>
			</section>

			<footer class="main" id="f1">
				<span><a href="https://es.wikipedia.org/wiki/Quiz" target="blank">¿Qué es un quiz?</a></span>
				<span>-</span>
				<span><a href="https://github.com/alejomg/quiz" target="blank">GitHub</a></span>
			</footer>

		</div>
	</body>
</html>